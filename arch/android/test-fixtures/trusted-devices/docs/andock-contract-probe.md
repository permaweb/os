# `andock-contract-probe@1.0`

Test-only, non-application consumer of the generic execution-device contract.
The Android runtime acceptance suite packages and loads this source after boot;
it is not included in the AndEE build or preloaded store.
