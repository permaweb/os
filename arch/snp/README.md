# Linux SNP Runtime

`make snp` builds the common Linux appliance with SNP support available through
`measurement@1.0` when the runtime is launched on real SEV-SNP hardware. The
Linux image remains otherwise shared with the no-TME runtime.
