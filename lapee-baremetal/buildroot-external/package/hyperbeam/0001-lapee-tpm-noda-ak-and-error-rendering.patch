diff --git a/native/lapee_tpm_nif/lapee_tpm_nif.c b/native/lapee_tpm_nif/lapee_tpm_nif.c
index f17d912a3..f57828a79 100644
--- a/native/lapee_tpm_nif/lapee_tpm_nif.c
+++ b/native/lapee_tpm_nif/lapee_tpm_nif.c
@@ -422 +422,8 @@ nif_create_signing_key(ErlNifEnv *env, int argc, const ERL_NIF_TERM argv[])
-    /* Template for restricted RSA-2048 signing key (PSS, SHA-256). */
+    /* Template for restricted RSA-2048 signing key (PSS, SHA-256).
+     *
+     * NODA is important for appliance attestations: this AK uses the
+     * build's empty object auth and must keep serving quotes even if the
+     * TPM's dictionary-attack counter is locked by unrelated firmware/user
+     * activity. Without TPMA_OBJECT_NODA, real Framework TPMs reject Quote
+     * with TPM_RC_LOCKOUT once global DA lockout is active.
+     */
@@ -431 +438,2 @@ nif_create_signing_key(ErlNifEnv *env, int argc, const ERL_NIF_TERM argv[])
-                TPMA_OBJECT_RESTRICTED | TPMA_OBJECT_SIGN_ENCRYPT,
+                TPMA_OBJECT_NODA | TPMA_OBJECT_RESTRICTED |
+                TPMA_OBJECT_SIGN_ENCRYPT,
diff --git a/src/dev_tpm2.erl b/src/dev_tpm2.erl
index b103737c4..3d7051758 100644
--- a/src/dev_tpm2.erl
+++ b/src/dev_tpm2.erl
@@ -1557 +1557 @@ error_resp(Status, Err, Reason) ->
-            <<"reason">> => hb_util:bin(Reason)
+            <<"reason">> => reason_to_text(Reason)
